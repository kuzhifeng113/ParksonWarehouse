<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色 -->
    <solid android:color="@color/white" />

    <!-- 边框的颜色和粗细 -->
    <stroke
        android:width="6dp"
        android:color="#F6A279"
        />
    <!-- android:radius 圆角的半径 -->
    <corners
        android:radius="10dp"
        />
</shape>